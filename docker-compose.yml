version: '3.9'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pds-comments-spa
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./staticfiles:/app/staticfiles
      - .:/app


